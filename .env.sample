# Sample environment variables for tatou
# Copy this file to .env and fill in the values as needed

# Database configuration
MARIADB_ROOT_PASSWORD=your_root_password    # pragma: allowlist secret
MARIADB_USER=tatou                          # pragma: allowlist secret
MARIADB_PASSWORD=tatou                      # pragma: allowlist secret

# GitHub token for private dependencies
GITHUB_TOKEN=your_github_token_here         # pragma: allowlist secret

# Other secrets and configuration
# Add any other environment variables required by your application below
